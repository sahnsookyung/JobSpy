services:
  jobspy-api:
    build: .
    container_name: jobspy-api
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    command: uvicorn api_server:app --host 0.0.0.0 --port 8000 --reload
